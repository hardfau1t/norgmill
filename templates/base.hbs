<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}}</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/prism/1.29.0/themes/prism.min.css">
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 20px;
        }
        header, footer {
            background-color: #f8f8f8;
            padding: 10px;
            text-align: center;
        }
        main {
            padding: 20px;
        }
        .button-container {
            text-align: left; /* Aligns content to the right */
            padding: 20px; /* Adds some spacing */
        }

        .home-button {
            background-color: #007bff;
            color: white;
            padding: 10px 20px;
            font-size: 16px;
            border: none;
            cursor: pointer;
            border-radius: 5px;
            text-decoration: none;
            display: inline-block;
        }

        .home-button:hover {
            background-color: #0056b3;
        }
        pre {
            padding: 15px;
            border-radius: 5px;
            background: #282c34;
            color: white;
            overflow-x: auto;
        }
        .block {
            padding: 1rem;
            margin: 1rem 0;
            border-radius: 4px;
            border-left: 4px solid;
        }
        .definition {
            background-color: #fff3e0;
            border-left-color: #ff9800;
        }

        .block-title {
            font-weight: bold;
            margin-bottom: 0.5rem;
        }
                .metadata {
            font-size: 14px;
            margin: 1rem 0;
            color: #4a5568;
        }

        /* Used to display document metadata */
        /* Minimal style */
        .metadata-minimal {
            display: flex;
            flex-wrap: wrap;
            gap: 0.5rem 1rem;
            padding: 0.5rem;
            background: #f8fafc;
            border-radius: 4px;
        }

        /* Subtle style */
        .metadata-subtle {
            border-left: 3px solid #e2e8f0;
            padding-left: 0.75rem;
            line-height: 1.6;
        }
        .metadata-subtle .meta-group {
            color: #64748b;
        }
        /* list items with status */
                .status {
            display: inline-block;
            width: 20px;
            margin-right: 8px;
            font-weight: bold;
        }
        .undone { color: #8e8e8e; }
        .done { color: #38a169; }
        .question { color: #805ad5; }
        .paused { color: #718096; }
        .urgent { color: #e53e3e; }
        .recurring { color: #3182ce; }
        .pending { color: #d69e2e; }
        .canceled { color: #4a5568; }
        .date {
            color: #666;
            font-size: 0.9em;
            margin-left: 8px;
        }
    </style>
</head>
<body>
    <p>
        {{{body}}}
    </p>
    <div class="button-container">
        <a href="/" class="home-button">Home</a>
    </div>
        <script src="https://cdnjs.cloudflare.com/ajax/libs/prism/1.29.0/prism.min.js"></script>
</body></html>
